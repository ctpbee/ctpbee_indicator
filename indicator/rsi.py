"""
    相对强度指数
    Formula:
          - up = upday(data)
          - down = downday(data)
          - maup = movingaverage(up, period)
          - madown = movingaverage(down, period)
          - rs = maup / madown
          - rsi = 100 - 100 / (1 + rs)
"""


class RSI:

    def upday(self, data:object):
        pass

    def downday(self, data:object):
        pass

